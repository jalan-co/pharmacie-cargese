---
/**
 * Bandeau de statistiques : carte bleu profond, 4 chiffres clés.
 */
import { stats } from '../data/site';
---

<section class="stats" aria-label="Chiffres clés">
  <div class="conteneur">
    <ul class="stats__carte">
      {stats.map((stat) => (
        <li class="stats__item">
          <span class="stats__valeur">{stat.valeur}</span>
          <span class="stats__libelle">{stat.libelle}</span>
        </li>
      ))}
    </ul>
  </div>
</section>

<style>
  .stats {
    /* Chevauche légèrement le hero au-dessus. */
    margin-top: calc(-1 * var(--esp-xl));
    position: relative;
    z-index: 2;
  }
  /* Bandeau résilient : s'adapte à n'importe quel nombre de chiffres,
     les blocs se répartissent et se centrent automatiquement. */
  .stats__carte {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--esp-lg) 0;
    padding: var(--esp-xl) var(--esp-lg);
    background: var(--bleu-profond);
    border-radius: var(--rayon-lg);
    box-shadow: var(--ombre-lg);
  }
  .stats__item {
    flex: 1 1 150px;
    max-width: 260px;
    display: flex;
    flex-direction: column;
    gap: var(--esp-2xs);
    padding-inline: var(--esp-lg);
    text-align: center;
  }
  .stats__item + .stats__item {
    border-left: 1px solid rgba(255, 255, 255, 0.14);
  }
  .stats__valeur {
    font-family: var(--police-titre);
    font-weight: var(--graisse-forte);
    font-size: var(--taille-xl);
    color: var(--blanc);
    line-height: 1;
    letter-spacing: -0.02em;
  }
  .stats__libelle {
    font-size: var(--taille-sm);
    color: var(--bleu-clair-3);
  }

  @media (max-width: 520px) {
    .stats__item {
      flex-basis: 100%;
      max-width: none;
    }
    .stats__item + .stats__item {
      border-left: none;
      border-top: 1px solid rgba(255, 255, 255, 0.14);
      padding-top: var(--esp-md);
    }
  }
</style>
